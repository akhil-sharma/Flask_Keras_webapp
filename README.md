# Flask_Keras_webapp
A simple Flask webapp to host a pretrained keras model.

This app accepts images in appropriate formats from the users and classifies these as Cat or Dog using the pre-trained keras cnn model.

~~The app is currently live at http://www.akhilsharmacse.com~~
